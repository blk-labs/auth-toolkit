# Admin dashboard demo

This app gives the login page a shared auth setup and provides a mock admin dashboard with a working logout button.

## Run it

From the repository root:

```bash
npm install
npm run build -w @auth-toolkit/core
npm run build -w @blk-auth-toolkit/react
npm run dev -w @auth-toolkit/admin-demo
```

